// Frames data temporarily disabled for Kiosk project
// Images were removed from the assets folder

export const SAMPLE_FRAMES: Array<{
  id: number;
  title: string;
  user: string;
  views: number;
  uses: number;
  image: string;
}> = [];
